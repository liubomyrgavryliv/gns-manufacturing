CREATE TABLE wf_models_list (
    id int AUTO_INCREMENT NOT NULL,
	type_name varchar(250),
	PRIMARY KEY (id)
);


CREATE TABLE wf_log (
    id int AUTO_INCREMENT NOT NULL,
	type_name varchar(250),
	PRIMARY KEY (id)
);